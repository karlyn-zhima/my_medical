我将提供给你一张超声图像，以及三张可能与他结构相似的超声图像，并配以对对应超声图像的分析：

图像提供的顺序将为：待分析的超声图像、相似图像1、相似图像2、相似图像3，对应超声图像的分析以json文本的形式提供，按照顺序分别是图像1分析、图像2分析、图像3分析。

重点：请根据提供了对应结构的三张超声图像进行学习，并理解层与层之间的关系，识别提供需要分析的超声图像中存在的各个层，但是分析结果中不要提到相似图像信息，相似图像中不存在的层，预测图片的预测结果中尽量不要出现。

需要识别的层应该狗仔相似图像的分析中，你只需要判断相似图像分析中存在的层该图像是否存在，如存在进行下列要求：

对于每个识别出的层，请回答以下内容：

1. 层的名称（根据超声图像特征和医学知识推断）；
2. 是否存在（true / false），这里应该默认为true；
3. 若存在，其在图像中的大致位置（如：上部、中部、下部；或从表皮向下第几层）；
4. 在超声图像中对应的视觉特征（仅基于图像实际表现描述回声强度、均匀性、边界等，不得编造或推测）；
5. 对于结节或病变，若存在，需描述其大小（以像素或相对尺寸说明，不进行数值估算，除非有标尺且可明确判断）。

分析要求：
- 你需要首先分析这张超声图像属于哪个部位的超声图像，其次确定该部位的超声图像会存在哪些内容，这里需要严格遵照相似参考图片和相似图片分析，不要有主观的层，一般来说，你需要输出的层和提供给你的相似分析的层比较一致相似分析的。
- 结合超声图像的回声特征识别解剖结构
- 根据实际图像内容确定存在的层，不要预设固定的层结构
- 你需要认真分析每个层是否存在，如果不存在，你需要给自己提供理由，如果应该存在，请一定存在；相同的，该层如果存在，也需要提供理由
- 关于层与层之间的顺序，请务必自己思考清楚，而不是根据提供给你的顺序判断层的顺序
- 相似图像中不存在的层，预测图片的预测结果中尽量不要出现

输出要求：

- 以 JSON 格式的字符串返回；
- 返回一个数组，数组中每个对象对应一个识别出存在的组织层；
- 每个对象包含字段：
  - layer（层名称，中文）
  - layer_en（层名称，英文）
  - exists（是否存在）
  - location（位置，中文）
  - location_en（位置，英文）
  - ultrasound_features（超声图像中的可观察特征，中文）
  - ultrasound_features_en（超声图像中的可观察特征，英文）
- 所有描述必须严格基于提供的超声图像内容以及提供的相似图像和对应图像的分析，不得引入推测或虚构信息。
- 中英文版本的内容必须保持一致，英文版本应准确翻译中文内容。
- 相似图像中不存在的层，预测图片的预测结果中尽量不要出现

请仅返回符合上述格式的 JSON 字符串，不要包含任何额外说明或解释。